/**
 * 저장 버튼 클릭 함수
 */
function handleSave() {}

/**
 * 오른쪽 화살표 버튼 클릭 함수
 */
function handleNext() {}

/**
 * 왼쪽 화살표 버튼 클릭 함수
 */
function handlePrev() {}

/**
 * 서비스 초기화 함수
 */
function init() {}

init();
