// 현재 선택된 옵션 상태 데이터
const options = {
  gender: undefined,
  age: undefined,
  questionNo: undefined,
};

/**
 * 선택된 옵션에 해당하는 그래프를 그리는 함수
 */
function drawGraph() {}

/**
 * 답변 별로 조건(age, gender, questionNo)에 맞는 데이터의 수를 계산하는 함수
 *
 * @ return {object} 답변 별, 데이터 수 ex - {a: 300, b: 100, c: 230}
 */
function countData() {}

/**
 * 초기화 함수
 * surveyData를 통해 옵션 버튼 생성
 */
function init() {}

init();
